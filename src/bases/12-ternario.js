
const activo = true

let mensaje = '';

// mensaje = activo ? 'activo' : 'inactivo';
// mensaje = activo ? 'activo' : null;

mensaje = activo && 'Activo'

// if (activo) {
//     mensaje = 'activo';
// } else {
//     mensaje = 'inactivo';
// }
console.log(mensaje);